---
// src/components/T.astro
// Usage: <T id="Teks Indonesia" en="English Text" tag="span" />
interface Props {
  id: string;
  en: string;
  tag?: string;
  class?: string;
}
const { id, en, tag = 'span', class: cls = '' } = Astro.props;
const Tag = tag as any;
---
<Fragment>
  <Tag data-lang-id class={cls || undefined}>{id}</Tag>
  <Tag data-lang-en class={cls || undefined}>{en}</Tag>
</Fragment>
